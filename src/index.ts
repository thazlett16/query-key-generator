export { createMetaKeys } from './create-meta-keys';
// export type { } from './create-meta-keys';

// const MODULE = {
//     USER: 'user',
//     EMPLOYEE: 'employee',
// } as const;
//
// const MODULES_VALUES = [...Object.values(MODULE)] as const;
// type Module_Value = (typeof MODULES_VALUES)[number];
//
// const MODULES_KEYS = [...Object.keys(MODULE)] as const;
// type Module_Key = (typeof MODULES_KEYS)[number];
//
// const OTHER = Object.freeze({
//     NAME: 'name',
//     AGE: 'age',
// } as const);
// const OTHER_LIST = Object.freeze([...Object.values(OTHER)] as const);
// type OTHER = (typeof OTHER_LIST)[number];
